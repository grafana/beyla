routes:
  unmatched: path
otel_metrics_export:
  endpoint: http://otelcol:4018
otel_traces_export:
  endpoint: http://jaeger:4318
discovery:
  services:
    - namespace: http2-go
      name: client
      exe_path: http2client
    - namespace: http2-go
      name: server
      exe_path: http2srv