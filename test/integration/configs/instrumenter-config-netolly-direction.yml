attributes:
  select:
    beyla_network_flow_bytes:
      include:
      - beyla.ip
      - src.address
      - dst.address
      - src.port
      - dst.port
      - src.name
      - dst.name
      - src.namespace
      - dst.namespace
      - src.cidr
      - dst.cidr
      - iface
      - direction
      - client.port
      - server.port
ebpf:
  bpf_debug: true
discovery:
  services:
    - exe_path: .
network:
  source: tc